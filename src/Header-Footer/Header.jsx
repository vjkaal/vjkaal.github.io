import React from "react";
import NavBar from "./NavBar";

import "./Header.sass";

const Header = () => {
  return (
    <header className="nav-up">
      <NavBar />
    </header>
  );
};

export default Header;
